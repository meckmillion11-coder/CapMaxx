import ListingDirectory from "@/components/ListingDirectory";

export const metadata = { title: "I Need – CapMaxx" };

export default function INeedPage() {
  return <ListingDirectory type="need" />;
}
