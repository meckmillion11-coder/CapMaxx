import EarlyAccessIntakeForm from "@/components/intake/EarlyAccessIntakeForm";

export default function FoundingCompaniesPage() {
  return (
    <EarlyAccessIntakeForm headline="Apply as a Founding Company on CapMaxx" />
  );
}
